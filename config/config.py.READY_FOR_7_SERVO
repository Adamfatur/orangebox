"""
╔═══════════════════════════════════════════════════════════════╗
║                    ORANGEBOX - CONFIG FILE                    ║
║                   READY FOR 7 POSITIONAL SERVOS               ║
╚═══════════════════════════════════════════════════════════════╝

✅ CONFIG INI SUDAH SIAP UNTUK 7 SERVO POSITIONAL (MG996R)
   Gunakan file ini saat servo baru sudah terpasang!

🔧 INSTALASI SERVO:
   CH 0  → Lock Left (MG996R positional)
   CH 2  → Lock Right (MG996R positional)
   CH 4  → Corner A (MG996R positional)
   CH 6  → Corner B (MG996R positional)
   CH 8  → Corner C (MG996R positional)
   CH 9  → Corner D (MG996R positional)
   CH 12 → Selector (MG996R positional)

📋 CARA AKTIVASI:
   1. Pasang semua 7 servo di channel yang benar
   2. Backup config.py lama: mv config.py config.py.old
   3. Aktifkan config ini: mv config.py.READY_FOR_7_SERVO config.py
   4. Test: python3 scripts/test_seven_servo.py

Copyright (c) 2025 AF - OrangeBox Project
"""

# ============================================
# CAMERA CONFIGURATION
# ============================================
PLATFORM = 'rpi'  # ← Set ke 'rpi' untuk Raspberry Pi
CAMERA_INDEX = 0
FANCY_UI = True

# ============================================
# MODEL CONFIGURATION
# ============================================
MODEL_PATH = 'models/model.tflite'
LABELS_PATH = 'models/labels.txt'

# ============================================
# CLASSIFICATION SETTINGS
# ============================================
CONFIDENCE_THRESHOLD = 0.5
MOTION_THRESHOLD = 3000
STABILIZATION_FRAMES = 5
MOTION_ENTER_THRESHOLD = 900
MOTION_EXIT_THRESHOLD = 450
MOTION_CONSECUTIVE_REQUIRED = 2
MOTION_STILL_FRAMES_RESET = 5

ENABLE_MORPHOLOGICAL_FILTER = True
MORPH_KERNEL_SIZE = 5

USE_MOG2 = True
MOG2_HISTORY = 300
MOG2_VAR_THRESHOLD = 16
MOG2_DETECT_SHADOWS = True

OBJECT_STABILIZATION_DELAY = 4.0
STABILIZATION_MAX_WAIT = 5.0
COOLDOWN_MS = 1500
SERVO_MOVEMENT_DURATION = 3.5

MIN_ROI_AREA_RATIO = 0.03
BINARY_DIFF_THRESHOLD = 18
DETECTION_ROI_Y_START_RATIO = 0.25
FIXED_COOLDOWN_ONLY = True
REARM_MS = 600

ENABLE_DISTANCE_ESTIMATION = False
FOCAL_LENGTH_PX = 900.0
KNOWN_OBJECT_HEIGHT_CM = 10.0

# ============================================
# HARDWARE SETTINGS (Raspberry Pi)
# ============================================
USE_PROXIMITY_SENSOR = False
PROXIMITY_SENSOR_PIN = 17

# ============================================
# SERVO CONFIGURATION - 7-SERVO SYSTEM
# ✅ READY FOR 7 POSITIONAL SERVOS (MG996R)
# ============================================
SERVO_DRIVER = 'seven_servo'

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# KALIBRASI PULSE WIDTH (MG996R OPTIMIZED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Range optimal untuk MG996R metal gear servo
# Mencegah servo hunting di batas mekanis (0°/180°)
SERVOKIT_MIN_PULSE_MICROS = 750   # Aman untuk MG996R
SERVOKIT_MAX_PULSE_MICROS = 2250  # Aman untuk MG996R
SERVOKIT_ACTUATION_RANGE = 180

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LAYER 1 - CORNER SERVOS (4 POSITIONAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Fungsi: Mengangkat/menurunkan wadah di 4 sudut
# Semua corner menggunakan angle yang SAMA untuk sinkronisasi sempurna

# Channel mapping (sesuai wiring fisik)
SERVO_L1_CORNER_A_CHANNEL = 4    # Sudut A (Kiri Atas)
SERVO_L1_CORNER_B_CHANNEL = 6    # Sudut B (Kiri Bawah)
SERVO_L1_CORNER_C_CHANNEL = 8    # Sudut C (Kanan Atas)
SERVO_L1_CORNER_D_CHANNEL = 9    # Sudut D (Kanan Bawah)

# Global default angles (SAFE RANGE: 10-100° untuk hindari servo hunting)
SERVO_L1_CORNER_UP = 10           # UP: Wadah terangkat (aman, jauh dari 0°)
SERVO_L1_CORNER_DOWN = 100        # DOWN: Wadah turun (90° swing, jauh dari 180°)

# Per-servo angles (SET SEMUA KE DEFAULT untuk sinkronisasi)
# ⚠️ PENTING: Gunakan nilai yang SAMA di semua corner untuk gerakan selaras!
SERVO_L1_CORNER_A_UP = None       # None = gunakan default (10°)
SERVO_L1_CORNER_A_DOWN = None     # None = gunakan default (100°)

SERVO_L1_CORNER_B_UP = None       # None = gunakan default (10°)
SERVO_L1_CORNER_B_DOWN = None     # None = gunakan default (100°)

SERVO_L1_CORNER_C_UP = None       # None = gunakan default (10°)
SERVO_L1_CORNER_C_DOWN = None     # None = gunakan default (100°)

SERVO_L1_CORNER_D_UP = None       # None = gunakan default (10°)
SERVO_L1_CORNER_D_DOWN = None     # None = gunakan default (100°)

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LAYER 1 - LOCK SERVOS (2 POSITIONAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Fungsi: Mengunci wadah di posisi atas (mencegah jatuh)
# Lock Left dan Right bisa berbeda arah (CCW vs CW)

# Channel mapping
SERVO_L1_LOCK_LEFT_CHANNEL = 0    # Lock Left
SERVO_L1_LOCK_RIGHT_CHANNEL = 2   # Lock Right

# Global default
SERVO_L1_LOCK_LOCKED = 90         # LOCKED: Horizontal, menahan wadah
SERVO_L1_LOCK_UNLOCKED = 10       # UNLOCKED: 80° swing (aman, jauh dari 0°)

# Per-servo angles (untuk arah berbeda)
SERVO_L1_LOCK_LEFT_LOCKED = 90
SERVO_L1_LOCK_LEFT_UNLOCKED = 10    # 80° swing CCW (aman)

SERVO_L1_LOCK_RIGHT_LOCKED = 90
SERVO_L1_LOCK_RIGHT_UNLOCKED = 170  # 80° swing CW (aman, jauh dari 180°)

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# LAYER 2 - SELECTOR SERVO (1 POSITIONAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Fungsi: Memilah sampah ke Bin A (Organik) atau Bin B (Anorganik)

SERVO_L2_SELECTOR_CHANNEL = 12    # Selector

# Angles (mid-range, aman)
SERVO_L2_SELECTOR_NEUTRAL = 90    # Tengah (horizontal)
SERVO_L2_SELECTOR_BIN_A = 60      # Bin A (30° ke kiri)
SERVO_L2_SELECTOR_BIN_B = 120     # Bin B (30° ke kanan)

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# TIMING CONFIGURATION (OPTIMIZED)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SERVO_LIFT_TIME = 0.3            # Waktu corner servos naik
SERVO_LOCK_DELAY = 0.1           # Delay setelah unlock/lock

SERVO_DROP_DELAY = 0.3           # Delay setelah selector posisi
SERVO_FALL_TIME = 0.5            # Waktu sampah jatuh Layer 1 → Layer 2
SERVO_SLIDE_TIME = 0.5           # Waktu sampah meluncur selector → bin
SERVO_RESET_DELAY = 0.3          # Delay setelah selector reset
SERVO_MOVEMENT_TIME = 0.15       # Waktu servo ke posisi target

# Stagger delay untuk distribusi power (30ms = smooth power draw)
SERVO_STAGGER_DELAY_MS = 30

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# TECHNICAL SETTINGS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SERVO_STOP_JITTER = True         # Matikan PWM setelah move (anti-jitter)
SERVO_PWM_FREQUENCY = 50         # 50Hz standard untuk hobby servo
SERVO_POSITION_HOLD_TIME = 0.05  # Hold time untuk mechanical lock

# PCA9685 I2C Settings
PCA9685_I2C_ADDRESS = 0x40       # Default I2C address
PCA9685_FREQUENCY = 50           # 50Hz PWM
PCA_CHANNELS_ONE_INDEXED = False # Board labeled 0-15 (not 1-16)

SERVO_DEBUG_TIMING = False       # Set True untuk debug timestamp
SERVO_KEEP_POWER_LOCKS = True    # Keep PWM on lock servos (strong hold)

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SERVO CALIBRATION (Fine-tuning per servo)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Gunakan untuk koreksi jika horn tidak bisa dipasang tepat 90°
# Nilai positif = putar CW, negatif = putar CCW
SERVO_OFFSET_LOCK_LEFT = 0
SERVO_OFFSET_LOCK_RIGHT = 0
SERVO_OFFSET_CORNER_A = 0
SERVO_OFFSET_CORNER_B = 0
SERVO_OFFSET_CORNER_C = 0
SERVO_OFFSET_CORNER_D = 0
SERVO_OFFSET_SELECTOR = 0

# Safety limits (tidak perlu diubah)
SERVO_MAX_SWING_LOCK_DEG = 90
SERVO_MAX_SWING_CORNER_DEG = 90

# Initialization behavior
SERVO_INITIALIZE_AT_START = False  # False = servo diam saat startup
SERVO_RESET_ON_SHUTDOWN = False    # False = hanya matikan PWM saat shutdown

# ============================================
# TIMING SETTINGS
# ============================================
STABILIZATION_DELAY = 0.5
SORTING_DURATION = 2.0
TRIGGER_COOLDOWN = 1.0

# ============================================
# GPS/LOCATION SETTINGS
# ============================================
ENABLE_GPS = False
GPS_UPDATE_INTERVAL = 300
GPS_MOCK_LOCATION = (-6.2088, 106.8456)
GPS_USE_IP_GEOLOCATION = False
GPS_SAVE_HISTORY = True
GPS_HISTORY_FILE = 'location_history.jsonl'
GPS_ENABLE_TIMEZONE = False

GPS_SERIAL_PORT = '/dev/serial0'
GPS_BAUDRATE = 9600
GPS_READ_TIMEOUT = 5
GPS_MIN_SATELLITES = 4
GPS_USE_GPSD = True

# ============================================
# DATABASE SETTINGS (MySQL RDS)
# ============================================
ENABLE_DATABASE = False
DB_HOST = 'orangebox.csxenzvznekp.ap-southeast-3.rds.amazonaws.com'
DB_USER = 'orangeboxmaster'
DB_PASSWORD = 'wpiGf0AtihhN2D3AjXUaSl'
DB_NAME = 'orangebox'
DB_PORT = 3306

# ============================================
# MULTI-DEVICE CONFIGURATION
# ============================================
DEVICE_ID = None
DEVICE_NAME = None
DEVICE_LOCATION = None
